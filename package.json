{
  "name": "@lgrammel/ds4-provider-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "build:ts": "pnpm -r build:ts",
    "changeset": "changeset",
    "clean": "pnpm -r clean",
    "release": "pnpm build && changeset publish",
    "typecheck": "pnpm -r typecheck",
    "version-packages": "changeset version",
    "format:check": "oxfmt --check \"packages/*/src/**/*.ts\" \"packages/*/test/**/*.ts\" \"examples/**/*.ts\"",
    "format:fix": "oxfmt --write \"packages/*/src/**/*.ts\" \"packages/*/test/**/*.ts\" \"examples/**/*.ts\""
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "oxfmt": "^0.48.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
